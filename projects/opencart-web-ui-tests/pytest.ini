[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -v --tb=short
markers =
    smoke: core smoke test cases
    regression: regression test cases
    optional: optional cases that may be unstable on public demo environment
